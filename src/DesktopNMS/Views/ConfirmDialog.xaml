<Window x:Class="DesktopNMS.Views.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:DesktopNMS.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:ConfirmDialogViewModel}"
        Title="{Binding Title}"
        Width="420"
        SizeToContent="Height"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Icon="pack://application:,,,/Assets/app.ico"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterOwner">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="24,24,24,10">
            <TextBlock Text="{Binding Title}" Margin="0,0,0,10">
                <TextBlock.Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource HeadingTextStyle}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsError}" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource CriticalBrush}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            <TextBlock Text="{Binding Message}" TextWrapping="Wrap" />
            <CheckBox Content="{Binding DontAskAgainLabel}"
                      IsChecked="{Binding DontAskAgain}"
                      Margin="0,18,0,0"
                      Visibility="{Binding ShowDontAskAgain, Converter={StaticResource BoolToVisibility}}" />
        </StackPanel>

        <Border Grid.Row="1"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="24,14">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel"
                        Command="{Binding CancelCommand}"
                        IsCancel="True"
                        Padding="16,7"
                        Margin="0,0,8,0"
                        Visibility="{Binding ShowCancel, Converter={StaticResource BoolToVisibility}}" />
                <Button Content="{Binding ConfirmLabel}"
                        Command="{Binding ConfirmCommand}"
                        IsDefault="True"
                        Padding="16,7">
                    <Button.Style>
                        <!-- No Cancel button to claim Escape when ShowCancel is
                             false (a plain OK-only notice) - this becomes the
                             Escape target too, so the dialog isn't stuck open
                             without a keyboard way to dismiss it. -->
                        <Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ShowCancel}" Value="False">
                                    <Setter Property="IsCancel" Value="True" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
