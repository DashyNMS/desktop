<Window x:Class="DesktopNMS.Views.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:DesktopNMS.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:ConfirmDialogViewModel}"
        Title="{Binding Title}"
        Width="420"
        SizeToContent="Height"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Icon="pack://application:,,,/Assets/app.ico"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterOwner">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="24,24,24,10">
            <TextBlock Text="{Binding Title}" Style="{StaticResource HeadingTextStyle}" Margin="0,0,0,10" />
            <TextBlock Text="{Binding Message}" TextWrapping="Wrap" />
            <CheckBox Content="{Binding DontAskAgainLabel}"
                      IsChecked="{Binding DontAskAgain}"
                      Margin="0,18,0,0"
                      Visibility="{Binding ShowDontAskAgain, Converter={StaticResource BoolToVisibility}}" />
        </StackPanel>

        <Border Grid.Row="1"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="24,14">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel"
                        Command="{Binding CancelCommand}"
                        IsCancel="True"
                        Padding="16,7"
                        Margin="0,0,8,0" />
                <Button Content="Confirm"
                        Command="{Binding ConfirmCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        IsDefault="True"
                        Padding="16,7" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
